# SSLENCRYPTED-CHATROOL
generate ssl certicifate with openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365
for linux users;
  pip3 install rich
for windows users;
  pip install rich
